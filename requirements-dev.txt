# Dépendances de développement / tests / CI (image fat, sans torch).
# Réunit RL tabulaire + tracking + serving + outils de test.
# torch est volontairement exclu : la suite de tests ne couvre pas le DQN
# (entraînement trop long pour une CI), d'où un build rapide.
gymnasium[toy-text]==1.3.0
numpy
matplotlib
mlflow==2.19.0
PyYAML==6.0.2
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pytest
httpx
